namespace darksky.Controllers {

    // No need to add the HomeController class to the main module as it can be directly used within ui.router $stateProvider.state()
    /*
    angular
        .module('darksky')
        .controller('HomeController', HomeController);
    */
    
    export class HomeController {

    }

}
